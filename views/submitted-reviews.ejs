<table class="table table-dark table-striped table-hover table-bordered border-light" id="submitted-reviews-table">
  <thead>
    <tr>
      <th>Employee</th>
      <th>Email</th>
      <th>Feedback</th>
    </tr>
  </thead>
  <tbody>
    <% submitted_reviews.forEach(review=> { %>

      <tr id="row-<%= review.forUser.id %>">
        <td class="user-name">
          <%= review.forUser.name %>
        </td>
        <td class="user-email">
          <%= review.forUser.email %>
        </td>
        <td>
          <i class="fas fa-eye" style="color: #038705" data-bs-toggle="modal" data-bs-target="#modal"
            onclick="viewFeedback(this,'<%- review.forUser.id %>')"></i>
        </td>
      </tr>

      <% }) %>
  </tbody>
</table>